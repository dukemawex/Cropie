# AgroSentinel Monorepo

This repository contains two production applications:

- `agrosentinel-frontend` — Next.js 14 App Router UI deployed to Vercel
- `agrosentinel-backend` — Node.js 18 Netlify Functions backend

## Repositories Structure

```text
.
├── agrosentinel-frontend/
└── agrosentinel-backend/
```

See each subproject README for setup, environment variables, endpoints, and deployment.
